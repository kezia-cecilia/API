{
  "name": "api-rast",
  "version": "1.0.0",
  "description": "conteudo de api via protocolo http",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rafa",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
